spring:
  application:
    name: product-determination
  cloud:
    consul:
      enabled: false
      discovery:
        enabled: false
        instance-id: ${spring.application.name}
        health-check-interval: 15s